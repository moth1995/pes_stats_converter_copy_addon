{
  "name": "pes-stats-copy-addon",
  "version": "1.3.10",
  "private": true,
  "description": "Test harness for the PES Stats Converter browser extension.",
  "type": "commonjs",
  "scripts": {
    "test": "node --test test/specs/",
    "test:golden": "node --test test/specs/golden.test.js",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.2.6",
    "typescript": "^5.9.3"
  }
}
